# meta-owned classpath defaults (task 16).
#
# Empty for now: with task 15 (splitting `CpmetaConfig` into shared/store-only/
# meta-only types) left undone, `rdfStore`'s own ConfigLoader.default still
# parses the entire `cpmeta` object, so every `cpmeta.*` default -- including
# fields only `meta` ever reads -- has to stay reachable from rdfStore's own
# module classpath and lives in rdf-common/src/main/resources/reference.conf
# instead (see the comment there). Once task 15 narrows the config type
# `rdfStore` actually parses, the meta-only `cpmeta.*` keys can move here.
